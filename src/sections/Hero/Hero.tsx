import "./Hero.css";

import HeroContent from "./components/HeroContent";

export default function Hero() {
    return (
        <section className="hero">
            <HeroContent />
        </section>
    );
}